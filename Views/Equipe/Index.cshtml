@model Equipe

@{
    ViewData["Title"] = "Equipe";
}

<h1>Tela de Equipes</h1>

<table class="table table-striped table-responsive">
    <thead>
        <th>Id</th>
        <th>Noem</th>
        <th>Imagem</th>
    </thead>

    <tbody>

        @foreach (Equipe equipe in ViewBag.Equipe)
        {
            <tr>
                <td>@equipe.IdEquipe</td>//usar o arroba para chamar a equipe
                <td>@equipe.Nome</td>
                <td>@equipe.Imagem</td>
            </tr>
        }

    </tbody>

</table>
